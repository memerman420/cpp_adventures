#include <iostream> // Allows us to read and write from the console
int main() {                            // every c++ function must have this, tell where to start, brackets define the "function body"
    std::cout << "Hello, World!" << std::endl; // prints to console and end line to be on new line after
    return 0;                           // indicates all is well
}



